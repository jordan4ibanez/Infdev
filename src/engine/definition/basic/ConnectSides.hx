package engine.definition.basic;

enum abstract ConnectSides(String) to String {
	var top = "top";
	var bottom = "bottom";
	var front = "front";
	var left = "left";
	var back = "back";
	var right = "right";
}
