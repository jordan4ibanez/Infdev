package entity;

class Player extends ObjectRef {
	// ? Begin baked in engine player entity features made nicer to use.
	//
	// Player object reference entity methods:
	//
	//* Begins: get_player_name
	//* Ends: set_flags
}
